// Integration i Recipe-detaljkomponent för att hantera rating och kommentarer

// src/RecipeDetails.wip.tsx
// WIP-placeholder: används inte av appen, men filen ligger kvar för framtida arbete.

import React from "react";

type Props = {
  recipeId: string;
};

const RecipeDetails: React.FC<Props> = () => {
  // Medvetet tom komponent. Returnerar null för att undvika alla TS-fel.
  return null;
};

export default RecipeDetails;